// import { store } from '../store';

// export async function fbStatusHandler(response) {
//     if (response.status === 'connected') {
//         localStorage.token = await response.authResponse.accessToken;
//         await this.$store.dispatch('authStore/logUserIn', response.authResponse);
//         await this.$store.dispatch('authStore/setSignType', 'facebook');

//         console.log(`only response from facebook: ${JSON.stringify(response)}`);
//     }
// }
